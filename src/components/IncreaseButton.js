import React from 'react'

const IncreaseButton = ({increaseButton}) => {
  return (
    
    <button className="btn btn-primary mx-2 mt-4" onClick={increaseButton}>+</button>
    
  )
}

export default IncreaseButton